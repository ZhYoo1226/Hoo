# OpenSolver开发日志

# 20260502

## 用户意图识别

### 步骤一：用户意图理解

用户意图理解触发meta:think
用户意图理解触发meta:speak
用户意图理解触发meta:listen
三选一行为：meta:think, meta:speak, meta:listen

### 步骤二：用户意图执行

三个状态：MetaListenState, MetaThinkState, MetaSpeakState



## 2026年5月3日

1. 执行提示词:owner.execute_prompt("文件名")。直接调用，自动填充模板：确保owner和g_store中有对应数据即可。
2. 初步确定Action的JSON结构

```json
{
  "action": "meta:listen",
  "params": {
    "text": "【继续聆听】",
    "type": "情绪:表达类"
  },
  "thought": "用户情绪表达，需要继续聆听用户反馈。"
}
```

3. meta:listen,meta:think,meta:speak的初步确立，完成ToolRegister到state状态的转换与执行。

   用例：

   ```python
   @g_tool_registry.register("meta:speak")
   def meta_speak(text: str, owner: StateOwner, **kwargs):
       print("响应QA直接回复状态")
       owner.add_state(MetaSpeakState(text))
   ```


## 2026年5月6日

1. 工具注册，函数注释
```python
@g_tool_registry.register()
def read_file(file_path: str,  # 文件路径
              max_read_size: int = 5 * 1024  # 最大读取字节数
              ):
    # 读取文件内容，不管他是什么文件。
    # 设置文件大小阈值（5KB），超过这个大小就返回摘要
```

2. 工具信息获取。

```python
tools = g_tool_registry.get_tools()
```



## 2026年5月10日
user/files/接收所有的文件，所有的解析都按照docx标准来解析（如果没有，则填无，不留白）。


## 2026年5月11日
1. action_json增加了多对```数据识别，除了第一个默认为action意外，其他后续的数据统统默认为data_list。
2. data_list的组装逻辑，如果是json就是直接解析，如果不是json就是直接按属性进行组装，暂且为list数组。



## 2026年5月14日
要求：提示词最后输出必须以###三个井号来开头，否则无法解析。已有的##需要整改，没有的需要调试输出###.
这个非常考验提示词的名称编写，如果名称不对，最后的输出无法输出【### 提示词名称】



## 2026年5月15日
1. 提示词解析逻辑，增加了```text```和```markdown```的解析。
 - text：直接返回文本内容。{"text": "文本内容",_llm_reponse_text:"llm回复内容"}
 - markdown：返回markdown格式的内容。{"text": "markdown内容",_llm_reponse_text:"llm回复内容"}
 - json：返回json格式的内容。{"key": "value",_llm_reponse_text:"llm回复内容"}



